{
    "name": "sample-vanilla-app",
    "version": "1.0.0",
    "private": true,
    "description": "",
    "license": "ISC",
    "author": "",
    "type": "module",
    "scripts": {
        "build": "parcel build index.html --public-url /vechain-dapp-kit/vanilla",
        "clean": "rm -rf dist .turbo",
        "dev": "rm -rf .parcel-cache && parcel index.html --port 5003",
        "e2e-build": "parcel build index.html ",
        "preview": "npx serve dist -p 5003",
        "purge": "yarn clean && rm -rf node_modules",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@vechain/dapp-kit-ui": "*"
    },
    "devDependencies": {
        "parcel": "^2.10.2"
    }
}
